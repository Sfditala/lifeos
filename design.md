# design.md — النظام البصري وقواعد الواجهة

هذا الملف مرجع ثابت يُقرأ قبل بناء أي صفحة أو مكوّن جديد، بدءًا من Step 0 في CLAUDE.md. أي قرار بصري غير مذكور هنا صراحة → توقف واسأل المستخدم بدل الاختراع، تمامًا بنفس منطق "قواعد صارمة" في CLAUDE.md.

---

## اللغة والاتجاه (i18n) — قرار يؤثر على بنية المشروع من Step 0

- **ثنائي اللغة: عربي (افتراضي) + إنجليزي.**
- المكتبة: `next-intl` مع بنية مسارات `app/[locale]/...`.
- ملفات ترجمة: `messages/ar.json` و `messages/en.json` — كل نص ثابت بالواجهة (أزرار، عناوين، رسائل، حالات فارغة) يُكتب هناك من أول يوم. **ممنوع أي نص عربي أو إنجليزي مكتوب مباشرة (hardcoded) داخل المكوّنات.**
- `<html lang={locale} dir={locale === 'ar' ? 'rtl' : 'ltr'}>` يتحدّد ديناميكيًا حسب اللغة الحالية.
- زر تبديل لغة ثابت بالشريط العلوي، يحفظ تفضيل المستخدم (cookie).
- الأيقونات الاتجاهية (أسهم رجوع/تقدّم، chevrons) لازم تنعكس تلقائيًا مع تبدّل الاتجاه — استخدم خصائص Tailwind المنطقية (`ms-`, `me-`, `ps-`, `pe-`, `text-start`, `text-end`) بدل القيم الثابتة (`ml-`, `mr-`, `text-left`) في كل مكان، حتى لا تنكسر الواجهة عند تبديل اللغة.

**اسأل المستخدم:** هل يحتاج تبديل اللغة فوريًا بدون إعادة تحميل الصفحة، أو تبديل عادي (إعادة تحميل مقبولة) كافٍ بالبداية؟

---

## الوضع الداكن/الفاتح

- **الافتراضي: داكن.** يبقى متاح مفتاح تبديل بسيط لوضع فاتح بالشريط العلوي (عبر `next-themes`)، يُحفظ تفضيل المستخدم محليًا.
- لا تستخدم أسود خالص (`#000000`) كخلفية أساسية — استخدم رماديًا داكنًا دافئًا (مثال قريب: `#0F1115` للخلفية، `#1A1D23` للبطاقات/الأسطح) لراحة العين في استخدام يومي وليلي متكرر.

---

## الخطوط

- العربي: **IBM Plex Sans Arabic** (عبر `next/font/google`).
- الإنجليزي/الأرقام اللاتينية: **Inter**.
- الأرقام: استخدم دائمًا الأرقام اللاتينية (0-9) حتى داخل النص العربي — أوضح للقراءة السريعة اليومية (تواريخ، عدّادات streak، أرقام) ما لم يطلب المستخدم غير ذلك.

**اسأل المستخدم:** يفضّل الأرقام الهندية (٠١٢) أو اللاتينية (012) في الواجهة العربية؟

---

## نظام الألوان

- **Base محايد** ثابت بغض النظر عن مجال الحياة: background / surface (بطاقات) / border / text-primary / text-muted — نفس القيم في كل الصفحات.
- **لون لكل مجال حياة (life_area.color):** عند إنشاء مجال حياة، يختار المستخدم لونه من لوحة مقيّدة (Curated palette) بدل منتقي ألوان حر — يضمن تباينًا جيدًا (WCAG AA على الأقل) فوق الخلفية الداكنة والفاتحة معًا. لوحة مقترحة (10 ألوان، Claude Code يحدد القيم الست عشرية الفعلية): أزرق، أخضر، برتقالي، بنفسجي، وردي، كهرماني/أصفر خردلي، تركواز، أحمر مرجاني، رمادي مزرق، بني فاتح.
- **استخدام اللون: تمييز فقط، مو تلوين كامل** — نقطة صغيرة (dot)، شريط جانبي رفيع على حافة البطاقة (`border-inline-start`)، أو خلفية شارة خفيفة الشفافية. لا تُلوَّن خلفية الصفحة الكاملة بلون المجال أبدًا.
- **ألوان الحالة** (منفصلة تمامًا عن ألوان المجالات، ثابتة بكل النظام): `todo` رمادي محايد، `doing` كهرماني/أصفر، `done` أخضر. الأولوية: `low` رمادي، `medium` أزرق فاتح، `high` أحمر/برتقالي.

---

## الكثافة وتخطيط الصفحة

التطبيق أداة تشغيلية يومية (أقرب لـ Linear/Todoist) وليس موقعًا تسويقيًا — لذلك:

- **كثافة متوسطة-مضغوطة** كافتراضي: صفوف قوائم (list rows) هي الشكل الافتراضي لصفحات البيانات (مهام، ملاحظات، محتوى)، وليس بطاقات كبيرة فارغة المساحة.
- ارتفاع صف القائمة يبقى مريحًا للمس على الجوال (~44-52px) حتى مع الكثافة العالية.
- استثناء متعمّد: الصفحة الرئيسية اليومية والتقويم الموحّد (صفحات "نظرة عامة/تأمل") تأخذ مساحة بصرية أوسع شوي من صفحات القوائم التفصيلية ("صفحات عمل/إدخال") — فرق مقصود بين النوعين، مو إهمال في الاتساق.
- التزم بمقياس التباعد الافتراضي لـ Tailwind (4px base) بدون تخصيص إضافي إلا لو ظهرت حاجة فعلية أثناء البناء.

---

## أنماط مكوّنات موحّدة (نفس الشكل في كل وحدة من CLAUDE.md)

- **البطاقة/الصف:** عنوان + وصف مختصر + شريط/نقطة لون مجال الحياة (إن وُجد) + إجراء سريع (checkbox أو قائمة ⋮) — نفس القالب لكل من: مهمة، هدف، مشروع، عنصر محتوى، ملاحظة، عادة.
- **الحالة الفارغة (Empty state):** أيقونة + جملة قصيرة + زر "أضف أول [كذا]" — لا صفحة فارغة بدون إرشاد أبدًا في أي وحدة.
- **شارة الحالة (Status badge):** مكوّن `Badge` من shadcn/ui بنفس ألوان الحالة الموحّدة أعلاه، في كل الوحدات بدون استثناء.
- **التنقل:** شريط جانبي (sidebar) على الشاشات الكبيرة، ينهار إلى قائمة سفلية أو زر قائمة (Sheet/hamburger) على الجوال — بما أن الاستخدام الفعلي اليومي غالبًا من الجوال.
- **الأيقونات:** `lucide-react` فقط (نفس مكتبة shadcn)، بدون خلط مكتبات أيقونات مختلفة بين الصفحات.

**قاعدة عامة تُطبَّق دائمًا:** أي مكوّن جديد يُبنى في أي Step، تأكد أنه يتبع نفس الأنماط أعلاه بدل ابتكار نمط بصري جديد لكل صفحة — الاتساق أهم من "الإبداع" في أداة استخدام يومي.
